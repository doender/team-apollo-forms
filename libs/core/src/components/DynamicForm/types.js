export var isFormField = function(item) {
  return item.type === 'formField';
};
export var isImageBlock = function(item) {
  return item.type === 'image';
};
export var isPlaceholder = function(item) {
  return item.type === 'placeholder';
};
//# sourceMappingURL=types.js.map
